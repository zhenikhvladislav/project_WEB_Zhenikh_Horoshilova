# Веб-приложение для создания резюме

## Team
- **Team Lead:** ФИО
- **Project Developers:** ФИО
- **Учитель:** Анатольев Алексей Владимирович

## 1. Описание проекта
Веб-приложение предназначено для создания резюме с возможностью выбора шаблонов и заполнения данных пользователем. Готовое резюме можно экспортировать в PDF.

## 2. Функциональные требования

### 2.1. WEB-сайт

#### Основные страницы:
- **Главная страница** (приветствие, краткое описание функционала).
- **Страница выбора шаблона резюме**.
- **Страница редактирования резюме** (ввод данных, настройка стилей).
- **Страница просмотра и экспорта в PDF**.

#### Элементы страниц (UI/UX):
- Поля ввода текста (имя, контакты, опыт работы и т. д.).
- Загрузка фото пользователя.
- Кнопки сохранения, экспорта и скачивания резюме.
- Превью резюме перед экспортом.

#### Шаблоны резюме:
- Минимум 3 варианта дизайна (**классический, креативный, строгий**).
- Поддержка CSS-стилей для настройки внешнего вида.

#### Регистрация и авторизация:
- Через **e-mail и пароль**.
- **OAuth** (Google, GitHub).

#### Работа с файлами:
- Загрузка изображений (аватарки).
- Сохранение данных пользователя.

#### API и интеграции:
- Генерация PDF (например, библиотека **jsPDF**).
- Поддержка **локального хранения или базы данных**.

#### Хранение информации:
- Данные пользователя (резюме) сохраняются в **локальном хранилище** или **базе данных**.
- Возможность **редактирования ранее созданного резюме**.

### 2.2. WEB-навыки и боты (при расширении проекта)
- Возможность взаимодействия с **ботом** (например, Telegram-бот для генерации резюме).
- **Команды** для создания резюме через текстовые команды.
- **Автоматические подсказки** по улучшению резюме.

## 3. Технологический стек
- **Frontend:** HTML, CSS, JavaScript (**React/Vue при необходимости**).
- **Backend** (если требуется база данных): Node.js + Express / Firebase.
- **База данных:** Firebase / PostgreSQL / локальное хранилище.
- **PDF-генерация:** jsPDF, html2canvas.

## 4. Требования к дизайну
- **Современный адаптивный UI**.
- **Поддержка мобильных устройств**.
- **Минималистичный дизайн** без лишних элементов.

## 5. Экспорт и сохранение
- **Резюме можно скачать в формате PDF**.
- **Доступна функция автосохранения**.

## 6. Дополнительно
- Возможность **добавления дополнительных разделов** в резюме.
- **Интерактивный превью-редактор**.
- **Поддержка нескольких языков**.

## 4. Структура проекта

### Возможная схема базы данных:
```
User {
  id: UUID,
  name: String,
  email: String,
  password: String,
  resumes: [Resume]
}

Resume {
  id: UUID,
  userId: UUID,
  template: String,
  content: JSON,
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

### Взаимодействие компонентов:
```
[Frontend (React/Vue)] <--> [Backend (Node.js, Express)] <--> [Database (Firebase/PostgreSQL)]
                                       |
                                       |--> [PDF Generation (jsPDF, html2canvas)]
```

_Все схемы и эскизы интерфейсов находятся в папке `materials` в формате PNG._

## 5. Описание кода

### a. Переменные и константы
- `USER_STORAGE_KEY` — ключ для локального хранилища.
- `PDF_OPTIONS` — настройки генерации PDF.

### b. Функции
- `saveResume(data)` — сохраняет данные резюме.
- `generatePDF(resumeData)` — создаёт PDF-файл.
- `uploadImage(file)` — загружает изображение аватарки.

### c. Классы
- `User` — управление пользователями.
- `Resume` — управление резюме.
- `PDFService` — генерация PDF.

### d. Библиотеки
- **Frontend:** React, TailwindCSS, Redux.
- **Backend:** Express, Firebase SDK, bcrypt.
- **Дополнительно:** jsPDF, html2canvas.

## 6. Графический интерфейс

_Эскизы интерфейсов находятся в папке `materials`._

## 7. Дедлайны